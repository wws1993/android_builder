name: Build Android APK

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v2

      - name: Install Cordova
        run: npm install -g cordova

      - name: Prepare Platform
        run: cordova platform add android --no-interactive

      - name: Build Android APK
        # 核心修改：通过环境变量直接告诉 Cordova 使用哪个版本的 Gradle
        # 这样就不需要 setup-gradle 插件，避开了 GitHub 缓存服务的报错
        env:
          CORDOVA_ANDROID_GRADLE_DISTRIBUTION_URL: https://services.gradle.org/distributions/gradle-8.7-all.zip
        run: |
          if [[ "${{ github.event.head_commit.message }}" == *"[release]"* ]]; then
            echo "模式: Release (生产环境)"
            cordova build android --release --verbose -- --apk
          else
            echo "模式: Debug (测试环境)"
            cordova build android --debug --verbose
          fi

      - name: Sign APK (Only for Release)
        if: success() && contains(github.event.head_commit.message, '[release]')
        uses: r0adkll/sign-android-release@v1
        id: sign_app
        with:
          releaseDirectory: platforms/android/app/build/outputs/apk/release
          signingKeyBase64: ${{ secrets.KEYSTORE_BASE64 }}
          alias: ${{ secrets.KEY_ALIAS }}
          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}

      - name: Upload Artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: my-app-apk
          path: |
            platforms/android/app/build/outputs/apk/**/*.apk
            ${{ steps.sign_app.outputs.signedReleaseFile }}